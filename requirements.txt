Project requirements for collaborators

This repository is a React/Vite frontend project.
It does not use Python or pip-based dependencies.

Required software:
- Git
- Node.js 22.x recommended
- npm 10.x or newer

Install project dependencies with:
cd react-frontend
npm install

Useful commands:
npm run dev
npm run lint
npm run typecheck
npm run build
